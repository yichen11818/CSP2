<Window x:Class="CSP2.Desktop.Views.Dialogs.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:CSP2.Desktop.Converters"
        mc:Ignorable="d"
        Title="{converters:Localization Dialog.ConfirmTitle}" 
        Height="280" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 图标和标题 -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource WarningLightBrush}"
                    Width="56"
                    Height="56"
                    CornerRadius="28"
                    VerticalAlignment="Top">
                <TextBlock x:Name="IconTextBlock"
                           Text="⚠️"
                           FontSize="32"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>

            <StackPanel Grid.Column="1" 
                        Margin="20,0,0,0"
                        VerticalAlignment="Center">
                <TextBlock x:Name="TitleTextBlock"
                           Text="确认删除"
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock x:Name="SubtitleTextBlock"
                           Text="此操作无法撤销"
                           FontSize="13"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,6,0,0"/>
            </StackPanel>
        </Grid>

        <!-- 消息内容 -->
        <Border Grid.Row="1"
                Background="{StaticResource SurfaceBrush}"
                CornerRadius="10"
                Padding="20"
                Margin="0,24,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="MessageTextBlock"
                           TextWrapping="Wrap"
                           FontSize="14"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           LineHeight="22"/>
            </ScrollViewer>
        </Border>

        <!-- 按钮 -->
        <Grid Grid.Row="2" Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="{converters:Localization Common.Cancel}"
                    Click="CancelButton_Click"
                    Style="{StaticResource SecondaryButtonStyle}"
                    HorizontalAlignment="Left"
                    Padding="24,12"
                    Width="120"/>

            <Button x:Name="ConfirmButton"
                    Grid.Column="1"
                    Content="{converters:Localization Common.Confirm}"
                    Click="ConfirmButton_Click"
                    Style="{StaticResource DangerButtonStyle}"
                    Padding="24,12"
                    Width="120"/>
        </Grid>
    </Grid>
</Window>

